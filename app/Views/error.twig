<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ title }} - error {{ code }}</title>
    <style>
        body {
            padding: 30px;
            font: 14px/1.5 Helvetica,Arial,Verdana,sans-serif;
            color: #000;
            font-weight: 400;
        }
        h1 {
            font-weight: inherit;
            line-height: 1;
            font-size: 40px;
            margin: 0 0 30px;
        }
        p {
            margin: 0 0 20px;
        }
        a {
            text-decoration: none;
            color: #1f33ff;
        }
    </style>
</head>
<body>
<div id="wrapper">
    <h1>{{ title }}</h1>
    {{ message|raw }}
</div>
</body>
</html>